---
import { slugifyToken } from "../lib/url";

interface Props {
  status: string;
  class?: string;
}

const { status, class: className } = Astro.props;
---

<span class:list={["status-badge", className]} data-status={slugifyToken(status)}>{status}</span>
